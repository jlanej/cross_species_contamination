name: CI – Extraction Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install samtools
        run: sudo apt-get update && sudo apt-get install -y samtools

      - name: Install package and test dependencies
        run: |
          pip install pysam
          pip install ".[test]"

      - name: Run tests
        run: pytest -v tests/
